# metrics for prometheus
# Only expose safe endpoints. Do NOT use '*' as it exposes /actuator/heapdump,
# /actuator/env (leaks secrets), and /actuator/shutdown (kills the process).
management.endpoints.web.exposure.include=health,info,prometheus
